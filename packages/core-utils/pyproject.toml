[tool.poetry]
name = "core-utils"
version = "1.0.0"
description = "Shared Python Utilities"
authors = ["Shadow Engineer Team"]
packages = [{include = "core_utils"}]

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
